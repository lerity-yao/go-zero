syntax = "v1"

type (
	// 用户结构体
	User {
		Name string `json:"name"` // 用户名称
	}
	Name {
		Code int `json:"code"`
	}
	Job {
		Id int `json:"id"`
	}
)

@server (
	// 分组 demoA
	group: demoA
)
service demoA {
	// 消费者 GDemoA
	@handler GDemoA
	post /test.direct.queue (Name)

	@doc "cron: */1 * * * *"
	@handler GDemoB
	post /cron.GDemoB
}

